Server for the Forged Cavalier Game.

Install:
--------
cd into server and run:
  npm install

Progress:
---------
So far, you can start up the server with:
  node server/app.js
Open a browser on localhost:8080. A message will be logged in the console, and
also in the server console.
